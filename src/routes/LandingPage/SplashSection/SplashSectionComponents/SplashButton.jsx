import React from 'react';
import '../index.scss';

const SplashButton = () => {
  return (
    <button type="button" className="splash-button">
      JOIN US
    </button>
  );
};

export default SplashButton;
