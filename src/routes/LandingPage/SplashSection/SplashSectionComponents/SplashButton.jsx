/* eslint-disable */

import React from "react";
import '../index.scss'

const SplashButton = () => {
  return (
    <button type="button" className="SplashButton">
      JOIN US
    </button>
  );  
};

export default SplashButton;